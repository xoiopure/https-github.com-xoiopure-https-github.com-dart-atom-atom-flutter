name: flutter_atomic_state
description: A Flutter package for atomically updated state.
version: 0.1.0
homepage: https://github.com/quantu-app/dart-flutter_atomic_state
repository: https://github.com/quantu-app/dart-flutter_atomic_state

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  atomic_state: "^0.1.1"
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter